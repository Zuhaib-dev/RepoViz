# Force serve static files first (200! means force, don't continue to other rules)
/sitemap.xml /sitemap.xml 200!
/robots.txt /robots.txt 200!
/favicon.ico /favicon.ico 200!
/*.png /:splat 200!
/*.jpg /:splat 200!
/*.webp /:splat 200!

# SPA fallback - serve index.html for all other routes
/* /index.html 200
