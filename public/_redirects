# Serve XML & text files as-is
/sitemap.xml   /sitemap.xml   200
/robots.txt    /robots.txt    200
/favicon.ico   /favicon.ico   200

# Static assets (Netlify handles these automatically – no rewrite needed)

# SPA fallback (LAST, always last)
/*             /index.html    200
